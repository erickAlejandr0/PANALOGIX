export function empresaChannel(empresaId: number) {
  return `empresa:${empresaId}`;
}

export function transportistaChannel(transportistaId: number) {
  return `transportista:${transportistaId}`;
}

export function viajeChannel(viajeId: number) {
  return `viaje:${viajeId}`;
}
